(in-package goal)

(deftype bpath-ctrl (process)
  (
    (active         symbol )
    (speed-m        meters ) ; per second
    (speed-t         float ) ; per second
    (progress        float )
  )
)
